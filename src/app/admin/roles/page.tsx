import { prisma } from "@/lib/prisma";import { getAdminSession } from "@/lib/admin-page-auth";import { AdminPage } from "@/components/admin/admin-page";import { AdminTable } from "@/components/admin/admin-table";
export default async function Page(){const session=await getAdminSession();const counts=await prisma.user.groupBy({by:["role"],_count:{role:true}});return <AdminPage user={session.user} title="Role Management" description="Role assignments are managed from Users and restricted to super administrators."><AdminTable title="Role summary" headers={["Role","Users","Permissions"]} rows={counts.map(x=>[x.role,x._count.role,x.role==="SUPER_ADMIN"?"Full access and role control":x.role==="ADMIN"?"Content, orders and users":"Purchased content and profile"])}/></AdminPage>}
